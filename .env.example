# Copy to `.env` or export these variables in your shell for local testing.
# Do NOT commit real secrets to the repo. Use GitHub Secrets for CI.

# OpenAI
OPENAI_API_KEY=

# ASR/LLM and runtime
ASR_ENABLED=0
DRY_RUN=1
SMOKE_TEST=1

# SMTP (optional - for sending email)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
MAIL_FROM=
MAIL_TO=
